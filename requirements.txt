# 作业自动上传工具 - 依赖包列表
# 安装命令: pip install -r requirements.txt

# GUI框架(内置,无需安装)
# tkinter

# Selenium浏览器自动化
selenium>=4.10.0

# 文件监控
watchdog>=3.0.0

# 文档解析
python-docx>=0.8.11    # .docx文件解析
pdfplumber>=0.9.0      # .pdf文件解析
olefile>=0.47          # .doc文件解析(Word 97-2003 OLE2格式)
pypdf>=3.0.0           # PDF文件合并
pywin32>=305           # Windows COM自动化(Word文档合并)

# HTTP请求(AI科目识别)
requests>=2.31.0

# 数据库(内置,无需安装)
# sqlite3

# 系统托盘
pystray>=0.19.0        # 系统托盘最小化
Pillow>=10.0.0         # 托盘图标生成

# GUI增强
tkinterdnd2>=0.3.0     # 拖拽文件上传支持
customtkinter>=5.2.0   # 现代化圆角UI控件库

# 数据可视化与统计
matplotlib>=3.7.0
openpyxl>=3.1.0

# Web API 服务（微信小程序对接）
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# 其他标准库(内置,无需安装)
# queue
# threading
# datetime
# time
# os
# json
# traceback
