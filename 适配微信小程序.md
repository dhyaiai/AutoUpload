# Python 后端项目改造开发指南
## 一、文档说明
本文档用于指导现有「作业自动上传工具」的服务化改造，在完全保留桌面端全部功能、双 Agent 能力的基础上，新增 FastAPI Web 接口层，对接微信小程序，实现移动端提交任务、查询状态、管理失败记录等能力。

### 改造原则
1. **低侵入**：不破坏原有桌面端核心逻辑，100% 复用上传处理器、浏览器自动化、AI 识别、双 Agent、数据库等模块
2. **数据互通**：桌面端 GUI 与小程序共用同一数据库、同一任务队列、同一浏览器实例，数据完全同步
3. **线程安全**：所有上传任务统一进入队列串行执行，从根源避免浏览器并发冲突、科目串位
4. **可服务化**：改造后可注册为 Windows 系统服务，支持开机自启、崩溃自动重启、无人值守运行

## 二、整体改造后架构

桌面端 GUI / 文件夹监控 → 任务队列 ← FastAPI 接口层 ← 微信小程序

 ↓ 

上传处理线程（单例浏览器）

 ↓ 

双 Agent 后台线程（自愈 + 分析）

 ↓ 

SQLite 数据库 / 本地文件存储

- 小程序提交的任务与桌面端任务进入同一队列，统一串行消费
- 失败自动接管 Agent、失败分析 Agent 全局生效，覆盖所有来源的任务
- 原有桌面端功能、文件夹监控功能完全保留，可同时使用

## 三、文件变更清单
### 新增文件
1. `api_server.py`：FastAPI 后端服务入口，包含生命周期管理、全部接口定义
2. 新增依赖：`fastapi`、`uvicorn`、`python-multipart`

### 修改文件
1. `db_manager.py`：扩展数据库字段、新增适配小程序的查询方法
2. `upload_processor.py`：适配小程序结构化任务、完善全流程阶段埋点
3. `browser_automation.py`：新增定时重启机制、强化环境复位方法
4. `config_manager.py` / `config.json`：新增服务端口、Agent 阈值等配置项
5. `auto_retry_agent.py`：适配多来源任务，保持原有分级自愈逻辑不变
6. `failure_analysis_agent.py`：支持手动指定时间范围生成报告

## 四、核心改造内容
### 1. 新增 FastAPI 接口层（api_server.py）
#### 生命周期管理
- 启动时：初始化数据库单例、浏览器单例，启动上传处理线程、双 Agent 线程、可选启动文件夹监控
- 停止时：触发全局停止事件，优雅关闭所有线程、退出浏览器
- 所有业务线程均为 daemon 线程，随主进程生命周期统一管理

#### 核心接口定义
| 接口路径                       | 方法 | 功能             | 入参                               | 核心返回                       |
| ------------------------------ | ---- | ---------------- | ---------------------------------- | ------------------------------ |
| `/api/upload/submit`           | POST | 提交上传任务     | school、grade、file、subject(可选) | task_id、提交状态              |
| `/api/upload/status/{task_id}` | GET  | 查询任务状态     | task_id（路径参数）                | 状态、科目、错误信息、重试次数 |
| `/api/failed/list`             | GET  | 分页获取失败列表 | page、size                         | 记录列表、总数                 |
| `/api/upload/retry/{task_id}`  | POST | 手动重试失败任务 | task_id（路径参数）                | 重试提交状态                   |
| `/api/report/generate`         | POST | 手动生成分析报告 | start_date、end_date               | 报告路径                       |
| `/api/stats/overview`          | GET  | 获取统计概览     | 无                                 | 总上传量、成功率、今日数据等   |

#### 接口规范
- 统一返回格式：`{"code": 0/非0, "msg": "描述", "data": 数据}`
- 全局开启 CORS 跨域，适配小程序调试
- 上传文件保存至 `./upload_temp` 目录，使用绝对路径入队

### 2. 数据库层适配
#### 表结构扩展（兼容旧数据，默认值均为 NULL）
在 `upload_records` 表新增以下字段：
- `fail_stage`：失败发生阶段，用于精准定位故障点
- `error_category`：错误一级分类
- `error_type`：错误二级类型，用于 Agent 决策
- `error_context`：JSON 格式错误上下文信息
- `retry_status`：重试处理状态（pending/processing/finished）
- `source`：任务来源（desktop/miniprogram）

#### 新增 DB 方法
- `add_pending_record()`：写入待处理任务记录
- `get_record_by_id()`：根据 ID 查询单条记录
- `get_failed_records()`：分页查询失败记录
- `update_retry_status()`：更新重试处理状态
- `get_failed_count()`：获取失败记录总数
- `get_stats_overview()`：获取统计概览数据

### 3. 上传处理器适配
- 兼容两种任务格式：文件夹监控的纯路径任务、小程序提交的带学校/年级参数任务
- 完善全流程阶段埋点：每个执行节点更新当前阶段，失败时自动写入 `fail_stage`
- 结构化错误写入：失败时按分类写入错误类型、上下文信息，不再仅存纯文本错误
- 任务执行完成后同步更新数据库，供小程序轮询查询

### 4. 双 Agent 适配
- **失败自动接管 Agent**：保持原有分级自愈、熔断保护、环境复位逻辑不变，扫描所有来源的失败任务；自愈过程与结果同步写入数据库，小程序端可实时查看
- **失败分析 Agent**：保持原有定时生成日报/周报逻辑，新增支持手动指定时间范围生成报告
- 所有 Agent 动作全程留痕，两端数据完全同步

### 5. 浏览器稳定性优化
- 新增定时重启机制：每处理固定数量任务自动重启浏览器，避免内存泄漏
- 保留 GUI 显示模式，本地部署无需强制无头模式，提升元素定位兼容性
- 强化 `reset_to_home()` 环境复位方法，重试前自动清理弹窗、复位页面状态

## 五、本地部署配置
### 1. 系统服务化
- 使用 NSSM 将服务注册为 Windows 系统服务
- 使用当前 Windows 用户账户运行，保障 Chrome 浏览器正常启动
- 配置崩溃自动重启，实现无人值守

### 2. 外网打通
- 使用 cpolar / 花生壳等内网穿透工具，映射本地 8000 端口至公网 HTTPS 地址
- 微信公众平台配置对应合法 request 域名

### 3. 环境优化
- 关闭电脑自动休眠、睡眠、硬盘自动关闭等节能选项
- 配置 SQLite 数据库、上传文件的定期备份
- 服务日志重定向至文件，方便排查问题

## 六、验收标准
1. 接口自测：通过 `/docs` 可完整走通上传、查询、重试、生成报告全流程
2. 功能兼容：原有桌面端全部功能正常，无回归问题
3. Agent 正常：失败任务可被自动接管，分级自愈、熔断保护生效
4. 服务稳定：注册为系统服务后，重启电脑可自动启动，运行正常
5. 数据互通：小程序提交的任务可在桌面端查看，两端数据一致