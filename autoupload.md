# 作业自动上传程序 设计文档（完整版）

## 1. 引言

### 1.1 项目背景

运营人员需将不同学校、年级的学生作业文件上传至指定在线教学平台。手动操作重复且易出错。本程序旨在构建一套桌面自动化工具，完成文件夹监听、AI科目识别、浏览器自动上传，并提供直观的图形管理界面，方便运营人员高效管理作业流程。

### 1.2 设计目标

- 自动监听大文件夹下以“学校+年级”命名的子文件夹中的新增文件。
- 从文件夹名解析学校与年级，读取文件前200字，通过 DeepSeek API 识别科目。
- 使用 Selenium 控制 Chrome 浏览器，自动完成登录、学校校验、文件上传等操作。
- 上传记录持久化到 SQLite 数据库，防止文件重复上传。
- 提供桌面 GUI 管理窗口（类似微信、QQ），可创建/删除文件夹、清空文件夹内文件并同步清理数据库记录。
- 支持短时间同一文件夹多文件场景，**复用同一个浏览器实例**，顺序上传且科目不混淆。
- 对上传失败的文件进行醒目提醒，并提供“重新上传”按钮，支持一键重试。
- 整体打包为单个 `.exe` 文件，双击即用。

### 1.3 运行环境

- 操作系统：Windows 10/11（64位）
- 浏览器：Google Chrome 及对应版本的 ChromeDriver
- 网络：需要能够访问目标教学网站和 DeepSeek API
- 数据库：SQLite（Python 标准库内置）

------

## 2. 系统总体设计

### 2.1 架构图

text

```
┌─────────────────────────────────────────────────────────────────┐
│                         app.exe (主程序)                         │
│                                                                 │
│  ┌──────────────────────────┐  ┌────────────────────────────┐   │
│  │   GUI 管理窗口           │  │  后台服务线程               │   │
│  │   (tkinter)             │  │                            │   │
│  │                         │  │  · 文件夹监控 (watchdog)    │   │
│  │  · 文件夹列表           │  │  · 任务队列                │   │
│  │  · 创建/删除按钮        │  │  · 上传处理器（单线程）     │   │
│  │  · 清空文件按钮         │  │    - 信息提取 & AI 科目识别│   │
│  │  · 失败文件列表         │  │    - Selenium 复用浏览器    │   │
│  │  · 重新上传/忽略按钮     │  │    - 写入数据库记录        │   │
│  │  · 实时日志显示         │  │                            │   │
│  └──────────┬───────────────┘  └──────────┬─────────────────┘   │
│             │                             │                     │
│             └──────────┬──────────────────┘                     │
│                        ▼                                        │
│            ┌─────────────────────┐                              │
│            │   SQLite 数据库      │                              │
│            │   (upload_records)  │                              │
│            └─────────────────────┘                              │
└─────────────────────────────────────────────────────────────────┘
```



### 2.2 模块划分

| 模块             | 职责                                                         |
| :--------------- | :----------------------------------------------------------- |
| **GUI 管理界面** | 基于 tkinter 构建，提供文件夹管理、失败文件处理、日志查看功能 |
| **文件夹监控**   | 使用 watchdog 监听子文件夹中的新增文件，将路径放入任务队列   |
| **信息提取**     | 解析文件夹名获取学校、年级；读取文件前200字内容              |
| **AI 科目识别**  | 调用 DeepSeek Chat API，根据前200字判断科目                  |
| **浏览器自动化** | 管理 Chrome 浏览器生命周期，复用实例完成登录、学校校验、文件上传 |
| **数据库**       | SQLite 存储上传记录，支持增删查，防重复上传，记录失败状态    |
| **主控制**       | 协调所有模块，启动 GUI 和后台线程，管理程序生命周期          |

------

## 3. 模块详细设计

### 3.1 桌面 GUI 管理界面

**技术**：Python 内置 tkinter + ttk 主题控件
**窗口标题**：作业自动上传管理工具
**窗口关闭行为**：安全停止后台线程，等待上传队列清空，关闭浏览器并释放资源。

#### 3.1.1 布局结构

text

```
┌──────────────────────────────────────────────────────────────┐
│ 作业自动上传管理工具                     [—][□][×]            │
├──────────────────────────────────────────────────────────────┤
│ 【创建新文件夹】                                              │
│ 学校名称：[________________]  年级：[▼ 高一]   [创建]       │
├──────────────────────────────────────────────────────────────┤
│ 文件夹列表：                                                 │
│ ┌─────────────────────┬───────────┬───────────────┐         │
│ │ 序号 文件夹名称      │ 清空文件   │ 删除文件夹    │         │
│ │ 1   合肥卓越中学高一 │ [🧹清空]   │ [🗑️删除]      │         │
│ │ 2   合肥一中高二     │ [🧹清空]   │ [🗑️删除]      │         │
│ └─────────────────────┴───────────┴───────────────┘         │
├──────────────────────────────────────────────────────────────┤
│ ⚠️ 上传失败文件（需处理）：                                    │
│ ┌─────────────────────┬──────────────┬───────────────────┐  │
│ │ 文件名              │ 失败原因      │ 操作              │  │
│ │ 物理作业.pdf        │ 元素定位超时  │ [🔄重新上传] [🗑️忽略]│
│ │ 英语听力.mp3        │ 格式不支持    │ [🔄重新上传] [🗑️忽略]│
│ └─────────────────────┴──────────────┴───────────────────┘  │
├──────────────────────────────────────────────────────────────┤
│ 运行日志：                                                   │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │[10:23:01] 监控已启动，浏览器已就绪                       │ │
│ │[10:23:15] 发现新文件：数学试卷.pdf                       │ │
│ │[10:23:18] 识别科目：数学 → 上传成功                     │ │
│ │[10:23:45] 发现新文件：语文作业.docx  → 排队等待          │ │
│ └──────────────────────────────────────────────────────────┘ │
│ 浏览器状态：🟢 已连接                                        │
└──────────────────────────────────────────────────────────────┘
```



#### 3.1.2 核心组件与功能

**1）创建文件夹区域**

- 学校名称输入框
- 年级下拉列表（高一、高二、高三、初一、初二、初三等）
- 创建按钮：拼接“学校+年级”作为文件夹名称，在根目录 `ROOT_DIR` 下新建，刷新列表。

**2）文件夹列表区域**

- 使用 `Treeview` 控件展示所有子文件夹，列：序号、文件夹名称。
- 每行添加两个操作按钮：
  - **清空**：弹出确认对话框，删除该文件夹内所有文件，并调用数据库接口删除该文件夹下的全部上传记录（无论成功/失败）。
  - **删除**：弹出确认对话框，删除整个文件夹（含所有文件），同步删除数据库对应记录。
- 操作完成后自动刷新列表。

**3）上传失败文件列表**

- 使用特殊底色（浅红色）的 `Treeview` 或 `Listbox`，展示所有 `status='failed'` 的记录。
- 显示列：文件名、失败原因、操作按钮。
- 操作按钮：
  - **重新上传**：将该文件路径重新放入上传队列头部，GUI中立刻移除该行（或标记为“重试中”）。上传成功后更新数据库状态；若再次失败，更新失败原因并重新显示。
  - **忽略**：删除该失败数据库记录，原文件保留。
- 支持最大重试次数限制（例如3次），达到上限后“重新上传”按钮变灰禁用。
- 列表无记录时显示“当前无失败文件”，背景色恢复普通。

**4）日志区域**

- 只读 `ScrolledText` 控件，实时显示监控、上传、操作日志。
- 使用 `logging` 模块的 `QueueHandler` 将后台日志传递到 GUI 线程。

**5）状态栏**

- 底部标签显示浏览器连接状态：🟢已连接 / 🔴未连接 / 🟡重启中。

**6）窗口关闭处理**

- 拦截关闭事件，调用停止信号 → 等待上传队列清空 → 关闭浏览器 → 关闭数据库连接 → 销毁窗口。

### 3.2 文件夹监控与任务队列

- **根目录**：由配置文件定义 `ROOT_DIR`，默认为程序同目录下的 `作业文件夹`。
- **监控库**：`watchdog`，递归监控。
- **事件过滤**：仅处理文件创建事件，且所在目录不是根目录本身。
- **任务队列**：`queue.Queue`，线程安全。
- **文件稳定等待**：文件创建后延迟 `FILE_STABLE_DELAY` 秒（默认2秒）再入队，避免读取未写入完成的文件。

### 3.3 信息提取

#### 3.3.1 学校与年级解析

从文件完整路径中获取父文件夹名，使用正则表达式匹配：

regex

```
^(.+?)(高一|高二|高三|初一|初二|初三|小一|小二|小三|小四|小五|小六)$
```



- 组1：学校名称
- 组2：年级
  若匹配失败，记录错误日志，并作为“未知学校/年级”处理（可人工在GUI中忽略或后续处理）。

#### 3.3.2 文件内容读取

支持常见格式，前200字符提取：

| 文件类型 | 解析库        | 方法                                                 |
| :------- | :------------ | :--------------------------------------------------- |
| `.txt`   | 内置 `open`   | `f.read(200)`                                        |
| `.docx`  | `python-docx` | 遍历段落累加文本至 ≥200 字                           |
| `.pdf`   | `pypdf`  | 逐页提取文本累加至 ≥200 字                           |
| 其他格式 | —             | 返回空文本，科目标记为“未知”，上传后作为失败记录处理 |

### 3.4 AI 科目识别

- **API 地址**：`https://api.deepseek.com/v1/chat/completions`

- **模型**：`deepseek-chat`

- **系统提示词**：

  text

  ```
  你是一个科目分类助手。根据提供的作业内容前200字，判断它属于哪个科目。
  仅回复科目名称，如：语文、数学、英语、物理、化学、生物、历史、地理、政治、信息技术等。
  ```

  

- **用户提示词**：`文件内容前200字：{text}`

- **请求参数**：`temperature=0`，保证输出稳定。

- **调用策略**：每个文件独立调用，绝不缓存或复用其他文件的结果。

- **重试机制**：网络异常、超时自动重试最多3次，间隔2秒；全部失败则科目设为“未知”，并将该文件记入失败记录。

### 3.5 浏览器自动化模块

#### 3.5.1 浏览器生命周期管理（关键改造）

- **启动**：程序初始化时在后台线程中启动 Chrome 实例，完成登录与权限选择，保持打开。
- **复用**：所有上传任务共享该浏览器实例，不反复开关。
- **自动重启条件**：
  1. 检测到登录态失效（页面出现重新登录弹窗）；
  2. 连续空闲超过 `BROWSER_IDLE_TIMEOUT` 秒（默认1800秒）；
  3. 程序退出时。
- **状态反馈**：GUI 底部标签实时显示浏览器是否可用。

#### 3.5.2 上传处理流程（单线程顺序消费）

上传处理器在一个独立线程中运行，循环从任务队列取出文件，执行：

1. 解析学校、年级。
2. 读取文件前200字 → 调用 DeepSeek 识别科目。
3. 检查文件名是否在数据库中标记为 `success`（防重复），是则跳过。
4. 执行浏览器操作：
   - 点击“上传作业”菜单/按钮。
   - 定位 `<input type="file">`，通过 `send_keys(file_path)` 上传文件。
   - 在下拉框中依次选择年级（`grade`）和科目（`subject`），文本完全匹配。
   - 设置预计使用时间为明天日期（`datetime.now() + timedelta(days=1)`）。
   - 提交表单。
   - **每步操作后固定 `time.sleep(0.5)`**。
5. 确认上传成功（检测成功提示或文件列表更新），调用 `add_record(status='success')`。
6. 异常处理：捕获所有异常，调用 `add_failed_record()` 写入失败记录，并向 GUI 队列发送刷新信号。
7. 循环处理下一个任务。

**防科目串位保障**：每个文件的科目均在上传前即时计算，且流程严格串行，确保不会误用其他文件的科目。

**浏览器状态检查**：每次上传前检查浏览器是否可用，若崩溃则自动重启并重新登录。

#### 3.5.3 学校校验机制

每次上传前必须进行学校一致性校验：

- 点击右上角教师名称，获取当前登录学校名称。
- 与目标文件对应的学校比较。
- 若不一致，点击“切换学校”并选择正确学校；若切换失败，记录错误并跳过该文件，将其标记为失败。

### 3.6 数据库模块

**数据库文件**：`data.db`，存放在程序目录。

**完整表结构**：

sql

```
CREATE TABLE IF NOT EXISTS upload_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    file_name TEXT NOT NULL,
    file_path TEXT NOT NULL,
    folder_name TEXT NOT NULL,
    school TEXT NOT NULL,
    grade TEXT NOT NULL,
    subject TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'success',   -- 'success' 或 'failed'
    error_message TEXT,                       -- 失败原因
    retry_count INTEGER DEFAULT 0,           -- 重试次数
    upload_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```



**核心接口**：

- `add_record(file_name, file_path, folder_name, school, grade, subject, status='success', error_message=None)`
- `is_file_uploaded(file_name)`：只判断是否存在 `status='success'` 的同名记录。
- `get_failed_records()`：返回 `status='failed'` 的所有记录。
- `mark_record_success(record_id)`：将失败记录更新为 `success`，清空错误信息，重置重试次数。
- `increment_retry(record_id)`：重试次数+1，超过阈值后 GUI 可禁用重试按钮。
- `delete_records_by_folder(folder_path)`：删除该文件夹下所有记录（不限状态）。
- `delete_record(record_id)`：删除单条记录（忽略操作）。

### 3.7 主控制与线程协调

**启动流程**：

python

```
def main():
    init_db()
    stop_event = threading.Event()
    
    # 启动后台工作线程（监控、上传处理）
    worker = threading.Thread(target=backend_worker, args=(stop_event,), daemon=True)
    worker.start()
    
    # 启动 GUI 主循环
    root = tk.Tk()
    app = MainApplication(root, stop_event)
    root.mainloop()
```



**后台工作线程**：

1. 初始化浏览器并登录。
2. 启动 watchdog 监控。
3. 进入上传处理循环（从队列取任务 → 上传），同时监听 `stop_event`。
4. 收到停止信号后，等待队列清空，关闭浏览器，退出。

**GUI 与后台通信**：

- 使用线程安全的 `queue.Queue` 传递日志消息和失败列表刷新指令。
- GUI 通过 `after(ms, callback)` 定时检查通信队列，更新界面。

------

## 4. 配置文件设计

在程序同级目录放置 `config.json`，内容示例：

json

```
{
    "ROOT_DIR": "./作业文件夹",
    "WEBSITE_URL": "https://example.com",
    "USERNAME": "admin",
    "PASSWORD": "123456",
    "ROLE": "teacher",
    "DEEPSEEK_API_KEY": "sk-xxxxxxxxxxxxxxxxxxxxxxxx",
    "CHROME_DRIVER_PATH": "./chromedriver.exe",
    "FILE_STABLE_DELAY": 2,
    "BROWSER_IDLE_TIMEOUT": 1800,
    "MAX_RETRY_COUNT": 3,
    "SLEEP_INTERVAL": 0.5
}
```



------

## 5. 技术选型

| 需求         | 技术方案                             |
| :----------- | :----------------------------------- |
| 桌面 GUI     | Python tkinter                       |
| 文件监控     | watchdog                             |
| 文档内容读取 | python-docx (docx), pypdf (pdf) |
| AI 科目识别  | DeepSeek Chat API (requests)         |
| 浏览器自动化 | Selenium WebDriver + Chrome          |
| 数据库       | SQLite3 (Python 内置)                |
| 打包         | PyInstaller                          |

------

## 6. 部署与打包

1. **准备环境**：Python 3.9+，安装依赖：

   bash

   ```
   pip install watchdog python-docx pypdf selenium requests pyinstaller
   ```

   

2. **准备驱动**：下载与 Chrome 版本匹配的 `chromedriver.exe`，放于程序目录（或使用 `webdriver-manager` 自动管理）。

3. **打包命令**：

   bash

   ```
   pyinstaller --onefile --console --name "作业自动上传工具" main.py
   ```

   

   - 若不需要控制台窗口可改为 `--noconsole`。
   - 可通过 `--add-binary "chromedriver.exe;."` 将驱动嵌入 exe。

4. **分发**：将生成的 `作业自动上传工具.exe` 与 `config.json`（如有）一同交付，用户双击即可运行。

------

## 7. 日志与异常处理

- **日志双输出**：`logging` 同时写入 GUI 日志区和本地文件 `app.log`。
- **异常捕获**：所有模块的顶级循环均包裹 `try...except`，记录完整堆栈，确保程序不崩溃。
- **重试机制**：
  - API 调用：3次重试，间隔2秒。
  - 浏览器操作：元素查找使用显式等待，超时则捕获异常，文件标记为失败。
- **失败闭环**：所有上传异常均捕获并写入数据库失败表，通过 GUI 醒目提醒并提供重试。

------

## 8. 完整需求实现总结

| 需求                   | 实现方式                                             |
| :--------------------- | :--------------------------------------------------- |
| 桌面 GUI 管理窗口      | tkinter 构建，包含文件夹管理、失败列表、日志、状态栏 |
| 文件夹创建/删除        | 在 GUI 中输入学校、年级，拼接后创建/删除文件夹       |
| 清空文件夹内文件并删库 | 遍历删除文件 + 调用数据库批量删除接口                |
| 文件监控与自动上传     | watchdog + 单线程队列消费                            |
| AI 科目识别            | 独立调用 DeepSeek，每个文件即时识别                  |
| 浏览器复用             | 启动时打开，任务共用，空闲超时或异常重启             |
| 防止科目串位           | 单线程处理，每个文件独立识别并立即上传               |
| 防重复上传             | 数据库文件名 + 状态校验                              |
| 上传失败醒目提醒       | GUI 红色背景失败列表，含失败原因和重试按钮           |
| 重新上传               | 将任务重新入队，上传成功后更新数据库状态             |
| 单 exe 部署            | PyInstaller 打包                                     |

------

## 9. 界面原型示意（补充）

（请参考前文布局示意图，此处不再重复绘制）

------

本设计文档全面涵盖了用户需求，从文件夹管理、自动化上传、失败处理到打包部署，各部分均有明确方案。开发团队可据此文档进行编码实现