# 🚀 快速打包指南 - 3步生成EXE

## ⚡ 最简单的方法

### 第1步: 双击运行 build.bat

```
找到文件: D:\python-project\AutoUpload\build.bat
操作: 双击运行
等待: 5-10分钟
```

### 第2步: 等待打包完成

你会看到类似这样的输出:

```
========================================
作业自动上传工具 - 打包脚本
========================================

[1/4] 检查Python环境...
Python环境正常

[2/4] 检查并安装依赖包...
正在安装 PyInstaller...
Collecting pyinstaller
...

[3/4] 开始打包(这可能需要几分钟)...
使用 build.spec 配置文件进行打包...
...

[4/4] 清理临时文件...

========================================
✓ 打包完成!
========================================

exe文件位置: dist\作业自动上传工具.exe
```

### 第3步: 找到生成的exe文件

```
位置: D:\python-project\AutoUpload\dist\作业自动上传工具.exe
大小: 约80-120MB
```

## 🎯 首次运行准备

### 1. 配置config.json

在 `dist` 文件夹中创建或修改 `config.json`:

```json
{
  "WEBSITE_URL": "https://school.7net.cn",
  "USERNAME": "你的用户名",
  "PASSWORD": "你的密码",
  "ROLE": "admin",
  "WATCH_FOLDER": "D:/upload",
  "SLEEP_INTERVAL": 1,
  "BROWSER_IDLE_TIMEOUT": 1800,
  "MAX_RETRY_COUNT": 3,
  "DEEPSEEK_API_KEY": "sk-xxxxxxxxxxxx"
}
```

**重要**: 
- 修改用户名、密码
- 设置正确的监听文件夹路径
- 申请DeepSeek API密钥(用于AI科目识别)

### 2. 创建监听文件夹

根据 `WATCH_FOLDER` 路径创建文件夹:

```
D:/upload/
─ 蚌埠第二中学高二/
│   ├─ 语文作业.docx
│   └─ 数学作业.docx
└─ ...
```

### 3. 运行exe

双击 `作业自动上传工具.exe`,打开GUI界面。

## ❓ 遇到问题?

### 问题1: 打包失败

**可能原因**: 
- Python未安装
- 网络问题导致依赖下载失败

**解决方法**:
1. 确保已安装 Python 3.8+
2. 检查网络连接
3. 手动安装依赖: `pip install -r requirements.txt`
4. 重新运行 `build.bat`

### 问题2: exe运行报错

**可能原因**:
- Chrome浏览器未安装
- config.json配置错误
- 监听文件夹不存在

**解决方法**:
1. 安装Chrome浏览器(版本90+)
2. 检查config.json配置
3. 创建监听文件夹
4. 查看日志输出(命令行运行exe查看错误信息)

### 问题3: 杀毒软件拦截

**解决方法**:
- 将exe添加到杀毒软件白名单
- 或暂时禁用杀毒软件

## 📞 需要帮助?

如果遇到问题:

1. **查看详细文档**: [BUILD_EXE_GUIDE.md](BUILD_EXE_GUIDE.md)
2. **查看日志**: 在命令行运行exe查看详细错误
3. **检查配置**: 确保config.json正确配置
4. **联系开发者**: 提供错误日志和截图

##  成功标志

当你看到GUI界面打开,说明打包成功!

```
┌─────────────────────────────────────┐
│  作业自动上传工具 v1.0              │
─────────────────────────────────────┤
│                                     │
│  [文件夹管理]                       │
│  - 监听文件夹: D:/upload            │
│  - 状态: ✓ 正常运行                 │
│                                     │
│  [实时日志]                         │
│  ✓ 程序启动成功                     │
│  ✓ 浏览器初始化完成                 │
│  ✓ 文件监控已启动                   │
│                                     │
└─────────────────────────────────────┘
```

---

**就这么简单!** 🎊
