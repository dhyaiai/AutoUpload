# 作业自动上传工具 - 架构设计总结

## 🎯 设计目标回顾

根据需求文档,本程序需要实现:
1. 自动监听文件夹中的新增作业文件
2. AI智能识别科目
3. 浏览器自动化上传
4. 图形化管理界面
5. 失败重试机制
6. 单文件打包部署

---

## 🏛️ 整体架构设计

### 架构模式: **分层模块化架构 + 生产者-消费者模式**

```
┌─────────────────────────────────────────────────────────┐
│                     用户交互层 (GUI)                      │
│                  gui_manager.py                          │
├─────────────────────────────────────────────────────────┤
│                    业务逻辑层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 信息提取模块  │  │ AI识别模块    │  │ 上传处理模块  │  │
│  │info_extractor│  │subject_class │  │upload_process│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    基础设施层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 数据库模块    │  │ 浏览器自动化  │  │ 文件监控模块  │  │
│  │ db_manager   │  │browser_auto  │  │file_monitor  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    配置管理层                              │
│                 config_manager.py                        │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 模块详细设计

### 1. **config_manager.py** - 配置管理模块

**职责**: 
- 读取和管理config.json配置文件
- 提供全局配置访问接口
- 支持默认值和动态修改

**设计模式**: 单例模式

**核心方法**:
- `get(key)`: 获取配置项
- `set(key, value)`: 设置配置项
- 属性访问器: `root_dir`, `username`, `password` 等

**技术要点**:
- 首次加载时合并默认配置和用户配置
- 配置修改后自动保存到文件
- 使用@property提供便捷的属性访问

---

### 2. **db_manager.py** - 数据库管理模块

**职责**:
- SQLite数据库的初始化和连接管理
- 上传记录的增删查改
- 防重复上传检查

**设计模式**: 单例模式

**核心表结构**:
```sql
upload_records (
    id, file_name, file_path, folder_name,
    school, grade, subject, status,
    error_message, retry_count, upload_time
)
```

**核心方法**:
- `add_record()`: 添加上传记录
- `is_file_uploaded()`: 检查是否已上传(防重复)
- `get_failed_records()`: 获取失败记录
- `mark_record_success()`: 标记为成功(重试后)
- `delete_records_by_folder()`: 批量删除记录

**技术要点**:
- 使用索引优化查询性能
- 参数化查询防止SQL注入
- 线程安全的连接管理(`check_same_thread=False`)

---

### 3. **info_extractor.py** - 信息提取模块

**职责**:
- 从文件夹名称解析学校和年级
- 读取文件前200字内容

**设计模式**: 静态工具类

**核心方法**:
- `parse_folder_name(folder_path)`: 正则表达式解析学校+年级
- `read_file_content(file_path)`: 根据扩展名选择读取方式

**支持的文件格式**:
- `.txt`: 直接读取
- `.docx`: 使用python-docx逐段读取
- `.pdf`: 使用pdfplumber逐页读取

**技术要点**:
- 正则表达式: `^(.+?)(高一|高二|...|小六)$`
- 累加文本直到达到200字符
- 异常处理和编码容错

---

### 4. **subject_classifier.py** - AI科目识别模块

**职责**:
- 调用DeepSeek API识别科目
- 支持重试机制

**核心方法**:
- `classify(text)`: 识别文本所属科目
- `_call_api(text)`: 实际调用API

**API参数**:
- 模型: `deepseek-chat`
- 温度: `0` (保证输出稳定)
- 系统提示词: 指导AI仅返回科目名称

**技术要点**:
- 最多重试3次,间隔2秒
- 超时时间30秒
- 清理AI返回结果中的标点符号

---

### 5. **browser_automation.py** - 浏览器自动化模块

**职责**:
- Chrome浏览器的生命周期管理
- 自动登录、学校切换、文件上传

**设计模式**: 单例模式

**核心方法**:
- `initialize()`: 启动浏览器并登录
- `check_and_switch_school(target_school)`: 校验并切换学校
- `upload_file(file_path, grade, subject)`: 执行上传
- `restart_browser()`: 重启浏览器
- `check_browser_status()`: 检查浏览器状态

**技术要点**:
- 复用浏览器实例,避免反复开关
- 空闲超时检测(默认1800秒)
- 登录态失效检测
- 显式等待和隐式等待结合
- ⚠️ **需要根据实际网站调整元素选择器**

---

### 6. **file_monitor.py** - 文件监控模块

**职责**:
- 使用watchdog监控文件系统变化
- 将新文件路径放入任务队列

**核心类**:
- `FileMonitorHandler`: 继承FileSystemEventHandler,处理文件创建事件
- `FileMonitor`: 管理Observer的启动和停止

**技术要点**:
- 递归监控根目录下的所有子文件夹
- 文件稳定等待(默认2秒),避免读取未完成文件
- 过滤根目录下的直接文件
- 线程安全的事件处理

---

### 7. **upload_processor.py** - 上传处理器模块

**职责**:
- 从任务队列取出文件并执行完整上传流程
- 处理失败情况和重试逻辑

**核心方法**:
- `run()`: 主循环,持续从队列取任务
- `_process_file(file_path)`: 处理单个文件的完整流程
- `retry_upload(record_id, file_path)`: 重新上传失败文件

**上传流程**:
```
1. 检查是否已上传(防重复)
2. 解析学校和年级
3. 读取文件内容
4. AI识别科目
5. 检查浏览器状态
6. 校验并切换学校
7. 执行上传操作
8. 记录成功/失败状态
```

**技术要点**:
- 单线程顺序处理,防止科目混淆
- 完整的异常捕获和错误记录
- 通过日志队列向GUI发送消息
- 支持最大重试次数限制

---

### 8. **gui_manager.py** - GUI管理界面模块

**职责**:
- 提供图形化用户界面
- 文件夹管理(创建/清空/删除)
- 失败文件显示和重试
- 实时日志显示

**技术栈**: tkinter + ttk

**核心组件**:
1. **创建文件夹区域**: 学校输入框 + 年级下拉框 + 创建按钮
2. **文件夹列表**: Treeview显示所有子文件夹,每行有清空/删除按钮
3. **失败文件列表**: Treeview显示失败记录,浅红色背景,有重试/忽略按钮
4. **日志区域**: ScrolledText实时显示日志,不同颜色区分类型
5. **状态栏**: 显示浏览器连接状态

**核心方法**:
- `_create_folder()`: 创建新文件夹
- `_refresh_folder_list()`: 刷新文件夹列表
- `_clear_folder()`: 清空文件夹并删除数据库记录
- `_delete_folder()`: 删除整个文件夹
- `_load_failed_records()`: 加载失败记录
- `_retry_upload()`: 重新上传失败文件
- `_update_logs()`: 定时从日志队列读取并显示

**技术要点**:
- 使用`after()`方法定时检查队列,确保线程安全
- 拦截窗口关闭事件,优雅退出
- 日志颜色分类(红色=错误,绿色=成功,蓝色=信息)
- 失败文件特殊底色提醒

---

### 9. **main.py** - 主控制模块

**职责**:
- 程序入口
- 协调所有模块的启动和停止
- 管理多线程架构

**线程架构**:
```
主线程: GUI主循环(tkinter)
  ↓
后台线程: backend_worker()
  ├─ 初始化数据库
  ├─ 启动浏览器
  ├─ 启动文件监控
  └─ 启动上传处理器(独立线程)
```

**核心函数**:
- `backend_worker()`: 后台工作线程,负责初始化和监控
- `main()`: 主函数,启动所有组件

**技术要点**:
- 使用`threading.Event`作为停止信号
- 使用`Queue`进行线程间通信
- 守护线程(`daemon=True`)确保程序能正常退出
- 优雅退出: 等待任务队列清空 → 停止监控 → 关闭浏览器 → 关闭数据库

---

## 🔗 模块依赖关系

```
main.py
  ├── config_manager.py (所有模块都依赖)
  ├── db_manager.py
  ├── file_monitor.py
  ├── upload_processor.py
  │     ├── info_extractor.py
  │     ├── subject_classifier.py
  │     ├── browser_automation.py
  │     └── db_manager.py
  ├── gui_manager.py
  │     └── db_manager.py
  └── browser_automation.py
```

---

## 🔄 数据流向

### 正常上传流程:

```
1. 用户放入文件到 "合肥卓越中学高一/" 文件夹
         ↓
2. watchdog检测到文件创建事件
         ↓
3. FileMonitorHandler将文件路径放入 task_queue
         ↓
4. UploadProcessor从队列取出文件
         ↓
5. InfoExtractor解析: school="合肥卓越中学", grade="高一"
         ↓
6. InfoExtractor读取文件前200字
         ↓
7. SubjectClassifier调用DeepSeek API识别科目 → "数学"
         ↓
8. BrowserAutomation检查并切换学校到"合肥卓越中学"
         ↓
9. BrowserAutomation执行上传操作(选择年级高一、科目数学)
         ↓
10. DatabaseManager记录: status='success'
         ↓
11. 日志发送到 log_queue → GUI显示"✓ 上传成功"
```

### 失败重试流程:

```
1. 用户在GUI失败列表中点击"重新上传"
         ↓
2. GUI调用 upload_processor.retry_upload(record_id, file_path)
         ↓
3. 增加重试次数,检查是否超过最大值
         ↓
4. 重新执行上传流程(步骤5-10)
         ↓
5a. 成功: 更新记录 status='success', 从失败列表移除
5b. 失败: 更新错误信息,保留在失败列表
```

---

## 🛡️ 关键设计决策

### 1. **为什么使用单例模式?**

- **DatabaseManager**: 避免多个连接导致的数据不一致
- **BrowserAutomation**: 复用浏览器实例,提高效率,避免频繁登录
- **ConfigManager**: 确保配置一致性,避免重复读取文件

### 2. **为什么使用队列通信?**

- **线程安全**: Queue是线程安全的,避免竞态条件
- **解耦**: 生产者和消费者不需要知道彼此的存在
- **缓冲**: 可以处理短时间内的突发任务

### 3. **为什么单线程处理上传?**

- **防止科目混淆**: 确保每个文件的科目识别和上传是原子操作
- **简化逻辑**: 不需要处理并发冲突
- **浏览器复用**: 单线程更容易管理浏览器状态

### 4. **为什么使用tkinter而不是其他GUI框架?**

- **内置库**: 无需额外安装,减小打包体积
- **轻量级**: 适合简单的管理界面
- **跨平台**: 虽然本项目只针对Windows,但tkinter天然跨平台

---

## ⚠️ 注意事项

### 1. **浏览器选择器适配**

`browser_automation.py` 中的元素选择器需要根据实际网站调整:
```python
# 这些选择器只是示例,必须修改!
By.NAME, "username"
By.ID, "login-btn"
By.CSS_SELECTOR, "input[type='file']"
```

### 2. **API密钥安全**

- 不要将包含真实API密钥的 `config.json` 提交到版本控制
- 建议使用 `.gitignore` 排除敏感配置文件

### 3. **ChromeDriver版本匹配**

- ChromeDriver版本必须与Chrome浏览器版本匹配
- 建议使用Selenium 4.x的自动驱动管理功能

### 4. **文件编码问题**

- 读取文本文件时使用 `errors='ignore'` 处理编码异常
- PDF和DOCX库通常能自动处理编码

---

## 📊 性能考虑

### 1. **数据库优化**

- 为常用查询字段创建索引(file_name, status, folder_name)
- 使用参数化查询避免SQL注入
- 批量操作时使用事务

### 2. **内存管理**

- 文件内容只读取前200字,避免大文件占用过多内存
- 日志队列设置合理大小,避免无限增长
- 浏览器及时关闭,释放资源

### 3. **网络请求优化**

- API调用设置超时时间(30秒)
- 失败重试间隔2秒,避免频繁请求
- 复用HTTP连接(requests库自动处理)

---

## 🧪 测试建议

### 单元测试重点:

1. **InfoExtractor**: 测试各种文件夹名称解析
2. **SubjectClassifier**: Mock API调用,测试重试逻辑
3. **DatabaseManager**: 测试所有CRUD操作
4. **ConfigManager**: 测试配置加载和保存

### 集成测试重点:

1. 完整的上传流程(从文件放入到上传成功)
2. 失败重试流程
3. 文件夹清空/删除同步数据库
4. 浏览器重启和重新登录

### 手动测试重点:

1. 不同文件格式的识别
2. 网络异常时的重试
3. 浏览器崩溃后的恢复
4. 大量文件同时放入的处理

---

## 📈 可扩展性

### 未来可能的改进:

1. **多浏览器支持**: Firefox, Edge
2. **更多文件格式**: .xlsx, .pptx, 图片文件
3. **批量上传**: 支持一次选择多个文件
4. **进度条**: 显示上传进度
5. **通知功能**: 上传完成弹窗或声音提示
6. **统计报表**: 上传成功率、各科目数量等
7. **云端同步**: 多设备共享上传记录
8. **插件系统**: 支持自定义网站适配器

---

## 🎓 学习要点

通过本项目可以学习到:

1. ✅ Python多线程编程和线程安全
2. ✅ 设计模式应用(单例、生产者-消费者)
3. ✅ SQLite数据库操作和索引优化
4. ✅ Selenium浏览器自动化
5. ✅ RESTful API调用和错误处理
6. ✅ tkinter GUI开发
7. ✅ 文件系统监控(watchdog)
8. ✅ 文档解析(docx, pdf)
9. ✅ 日志系统和异常处理
10. ✅ PyInstaller打包部署

---

## ✨ 总结

本项目采用**模块化分层架构**,将复杂的功能分解为9个独立的模块,每个模块职责明确、易于维护和测试。通过**单例模式**管理共享资源,**队列通信**保证线程安全,**单线程处理**防止数据混乱,构建了一个稳定可靠的自动化上传工具。

**核心优势:**
- 🎯 职责清晰: 每个模块只做一件事
- 🔒 线程安全: 队列通信,避免竞态
- 🔄 容错性强: 完善的异常处理和重试机制
- 📦 易于部署: 打包为单个exe文件
- 🎨 用户友好: 直观的图形界面

**适用场景:**
- 教师批量上传学生作业
- 教育机构自动化办公
- 任何需要浏览器自动化的场景

---

**架构设计完成! 🎉**
